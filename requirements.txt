# Core dependencies
requests>=2.31.0
# Upper bound is deliberate. This code targets the mcp 2.x API
# (mcp.server.mcpserver.MCPServer). The old unbounded `mcp>=0.9.0` silently
# resolved to 2.x after the FastMCP -> MCPServer rename, which broke the server
# on import and left it crash-looping under systemd -- and every `rmenv/mkenv`
# rebuild reproduced it, because nothing pinned the major.
mcp>=2.0,<3

# Image processing (required for view_image tool)
Pillow>=10.0.0

# Testing (optional, for development)
# Note: pytest-asyncio only needed if you add async tests
pytest>=7.4.0
# pytest-asyncio>=0.21.0  # Uncomment if adding async tests (may have version conflicts)